import { green } from "colorette";

// this is the basic config for the application

const config = {
  API_URL: "https://infinite-dawn-93085.herokuapp.com",
  colors: {
    inUse: "#32CD32",
    inRest: "#FFA500",
    available: "#f5f5f5"
  }
}

export default config
